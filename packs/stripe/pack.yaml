name: stripe
version: "0.1.0"
permit0_pack: "permit0/stripe"
vendor: stripe
description: "Normalizer and risk rules for Stripe API tool calls"
normalizers:
  - normalizers/charges_create.yaml
  - normalizers/refund.yaml
risk_rules:
  - risk_rules/charge.yaml
