permit0_pack: "permit0/email"
id: "email:gmail_set_forwarding"
priority: 128
match:
  tool: gmail_set_forwarding
normalize:
  action_type: "email.set_forwarding"
  domain: "email"
  verb: "set_forwarding"
  channel: "gmail"
  entities:
    destination:
      from: "forwarding_email"
      type: "string"
      required: true
    enabled:
      from: "enabled"
      type: "boolean"
      optional: true
